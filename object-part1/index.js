const user = {
  name : "sita",
  age: 25,
  country: "India",
};

const keys = Object.keys(user);
console.log(keys);    //accesses of keys

const values = Object.values(user);
console.log(values);   //accesses of values

const entries = Object.entries(user);
console.log(entries);